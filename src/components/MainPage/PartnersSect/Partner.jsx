import { FontAwesomeIcon } from '@fortawesome/react-fontawesome';

const Partner = (props) => {
  return <FontAwesomeIcon icon={props.icon} className='mx-14 animate-slide' />;
};

export default Partner;
